const baseURL = 'https://rickandmortyapi.com/api';

const episode = `/episode`;
// const character = '/character';

const urls = {
    episodes: {
      base: episode
    }
}

export {
    baseURL,
    urls
}